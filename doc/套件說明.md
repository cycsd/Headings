
## MindMapMd 的主要目的

MindMapMd 的核心目的，不是把 Markdown 變成另一套格式，而是提供一個**以 Markdown 原始結構為基礎的視覺化編輯方式**。使用者仍然編輯同一份 Markdown 文件，但可以用更接近 mind map 的方式理解目前的階層、群組關係與段落位置。

具體來說，這個套件主要要解決的是：

- 讓使用者更容易看出 Heading、Paragraph 與子群組之間的從屬關係。
- 讓原本在純文字 Markdown 中不容易觀察的「同層關係」、「群組邊界」與「上下文位置」變得直覺。
- 提供比原生 Markdown 更容易操作的結構調整方式，例如移動區塊、升降階層、合併區塊與 drag and drop。
- 在視覺化操作的同時，仍然回寫成正常可讀、可維護的 Markdown，而不是引入另一套資料格式。
- 幫助使用者在長文或多層級筆記中，降低因結構複雜而造成的編輯成本與理解成本。

換句話說，MindMapMd 比較像是 **Markdown 的結構化視圖與操作層**，而不是取代 Markdown 的編輯器。它的價值在於：保留 Markdown 的可攜性與可讀性，同時補上原生 Markdown 在大型結構編輯上的不足。

## Mind Map MD View Layout

當使用者開啟 Mind Map View 時，套件不會把 Markdown 轉換成另一種儲存格式，而是把原本線性的 Markdown 內容，重新投影成一個更容易閱讀結構的視覺化版面。

在原始 Markdown 中，Heading、Paragraph 與不同層級的內容，都是依照單純的上下文字順序排列。這種形式雖然易於編寫，但當文件變長、層級變多時，使用者不容易快速看出：

- 哪些段落屬於哪個 Heading
- 哪些區塊處於同一層
- 哪些內容是平行群組，哪些內容是某個區塊的子內容
- 如果要移動、合併或調整階層，實際上會影響哪一段結構

Mind Map View 的作用，就是把這種線性的文件，轉成一種接近 mind map 的排列方式，讓使用者可以直接從版面看出「主節點」、「子節點」、「同層節點」與「群組邊界」。

在這個視圖中：

- 文件本身會作為整體的根節點
- Heading 會作為上級節點
- Paragraph,List item, quote..等內容，會作為 Leaf 節點

也就是說，Mind Map View 不是單純把 Markdown「畫成方框」，而是把 Markdown 的文件結構重新表達成可操作、可閱讀的空間關係。這樣做的目的，是讓使用者在保留原本 Markdown 編輯模型的前提下，更直覺地理解與操作複雜結構。

下面這個例子說明的是：同一份 Markdown 文件，如何從原本的線性文字排列，映射成 Mind Map View 中的視覺化布局。

原始 Markdown：
```markdown
yaml
# HeadingA
paragraph_a1
## Heading_A2
paragraph_a2
# HeadingB
#### Heading_B4
paragraph_b1
paragraph_b2
### Heading_B3
paragraph_b3
# HeadingC
paragraph_c1
```

對應的 Mind Map View：
```text
                                         +----------------+
                                         | paragraph_a1   |
                                         +----------------+
                        +------------+   +----------------+   +----------------+
                        | # HeadingA |   | ## Heading_A2  |   | paragraph_a2   |
                        +------------+   +----------------+   +----------------+
+-------------------+   +------------+   +----------------+   +----------------+
| Document Name     |   | # HeadingB |   | #### Heading_B4|   | paragraph_b1   |
| yaml              |   +------------+   +----------------+   +----------------+
+-------------------+   +------------+   +----------------+   +----------------+
                        | # HeadingC |   | ### Heding_B3  |   | paragraph_b2   |
                        +------------+   +----------------+   +----------------+
                                         +----------------+   +----------------+
                                         | paragraph_c1   |   | paragraph_b3   |
                                         +----------------+   +----------------+
``` 




## 可使用套件

### CodeMirror
[CodeMirror 6](https://codemirror.net/)
[svelte-codemirror-editor](https://github.com/touchifyapp/svelte-codemirror-editor)

[codemirror-live-markdow](https://github.com/blueberrycongee/codemirror-live-markdown)
目前有 Heading 前面會顯示 _ 的問題，及 list item 前方不會顯示 · 的問題

[ProseMark](https://github.com/jsimonrichard/ProseMark/tree/dev)
目前的問題在於套件使用的 codemirror 版本與 codemirror 本身不匹配

### Drag And Drop Combine VirtualList
[pragmatic-dnd](https://github.com/rares-fodor/pragmatic-dnd-virtual-example)
[svelte-tanStack-example](https://github.com/rares-fodor/pragmatic-dnd-virtual-example)

### Drag And Drop
[Fluid DnD](https://fluid-dnd.netlify.app/svelte/?ref=madewithsvelte.com)
[Flexiboards](https://www.flexiboards.dev/)
[swapy](https://swapy.tahazsh.com/)
[hello-pangea](https://github.com/hello-pangea/dnd/tree/main)

### Virtual List
需要提供 scrollto 功能且要置中呈現
[virtua](https://github.com/inokawa/virtua)
[jonasgeiler/svelte-tiny-virtual-list: 📚 A tiny but mighty list virtualization library for Svelte, with zero dependencies 💪 Supports variable heights/widths, sticky items, scrolling to index, and more!](https://github.com/jonasgeiler/svelte-tiny-virtual-list)

[Humanspeak | AI Software Development, AI Consultancy, UI Design](https://humanspeak.com/)

### Keybindings
[tinykeys](https://github.com/jamiebuilds/tinykeys)

## 名詞解釋


## 基本邏輯
如果在同一層(同一個 Column)

對於同個群組下面的才適用...
上面的階層肯定比下面的小，因為如果在同一層，如果你的階層又比下面的大，那麼下面的 Block 理應是你的下一層

ex:
可能有這樣的 markdown 結構
```markdown
# Heading1
### Heading3
## Heading2

```
那麼 Heading 3 與 Heading 2 會在同一層，且上面的 Block Heading3 肯定比 下面的 Block Heading2 階層還小

```text
# Heading 1 - ### Heading3
            - ## Heading2
```


如果是這樣的 markdown 結構
```markdown
# Heading1
## Heading2
### Heading3

```
呈現方式要是這樣
```text
# Heading 1 - ## Heading2 - ### Heading3
            
```

而不是這樣
```text
# Heading 1 - ## Heading2
            - ### Heading3
```

但如果是不同群組，則在同一層 還是會有上面的區塊 level 比下面大的情況，
**或許是一種可以用來判定是否是群組邊界的方法？**
Heading B 屬於 Heading A 群組，
paragraph c 屬於 Heading C 群組，
而依照架構 Heading B 與 paragraph c 在同一層(Column)。
```markdown
## Heading A
### Heading B
paragraph a
paragraph b
## Heading C
paragraph c

```
呈現方式會是

```text
## Heading A - ### Heading B   - paragraph a
                               - paragraph b
## Heading C - paragraph c
```


## 目前待驗證的問題點
### 同步問題
如何在外部改動目前瀏覽的文件時，保持使用者目前選擇的 Block，而不會因為段落刪除或移動而變成別的 Block 被 selected
#### Solution
本來從外部將檔案重寫就不能 100% match 原本選擇的 Block，
例如將檔案刪到空白，那麼我也只能將標題當作目前選擇的 Block。

故應該提供不同的策略去保持使用者原本選取的 Block
- Block content match - 最符合，只要內容一致代表使用者只是改動其他區塊沒有動到目前所選擇的區塊，但如果文件內有重複區塊就無法區分，需要在用其他方式找最符合的
- Road Map - 適用於整個群組未調整，且有重複的區塊，或當前區塊是 Paragraph 且使用者改動的是當前區塊
- 相對應位置 -當前使用者是選擇整個文件的第 6 段，則即使使用者調整後，還是會在第 6 段，適用於使用者調整 Heading 名稱，其他版面配置沒有調整，或調整的區塊是在當前選擇的區塊的後面，但其實這已經偏向儘可能的復原的策略了。

給 Key Id？ 但要用什麼當作 Key ID ，如果有重複名稱的段落該怎麼辦？
ex: 重複的標題名稱 
用Road Path?  ex: 同步問題 -> Solution
監聽改動？ 如何監聽？

### 移動問題
#### Solution

### 復原問題
如何提供 Ctrl + Z 的功能給使用者，
主要因為使用者除了手打之外還可以用 short command 及 drap and drop，
的功能調整文字 offset，

目前想法是可以靠 codemirror 預設提供的 history 功能，
但如何將 history 的資訊還原成使用者的操作？

ex: 使用者不小心拖拉 Block 到錯誤的群組內，想要用 Ctrl + Z 來做一鍵復原，
將這個 Block 回復到它原本應該在的位置。

### 顯示問題
隱藏 scroll-bar

## 功能
### Core
基本視圖
     unselected - unselected - unselected
        road    -   road     - fork - | - path1
                                      | - path2
                                      | - path3
     unselected - unselected - unselected
#### 方向
基本上，應以原本 markdown 的操作為主，並在提供原本 markdown 難以操作的功能
ex:
##### *markdown 原有功能*
可以簡單的上下移動區塊
##### *markdown 難以實現功能*
###### 整段 Heading 階層 shift
對於長文很難每個 Heading 都 shilft 一個階層
如果 Mind Map View 提供整段 Heading shift ，使用者就不用一個一個 Heading 慢慢調整
ex:
```markdown
## Heading2
paragraph a
### Heading3
paragraph b
#### Heading4
```
整段 shilft 後

上升階層
```markdown
# Heading2
paragraph a
## Heading3
paragraph b
### Heading4
```
下降階層
```markdown
### Heading2
paragraph a
#### Heading3
paragraph b
##### Heading4
```


#### 當前選擇 Block 置中
#### 呈現 Breadcrumb

### Short Command
可以以組合鍵的方式進行，
ex:
paragraph 的右移可以先是代表將 paragraph 移到群組的第一個，
而如果 paragraph 已經是第一個，再次輸入右移，則等同上移一個，移到上方群組的最後一個。

或者也可依照目前的位置來做調整 short command 的意義
#### 升降層級 (= 移動區塊？ 還是移動區塊可以拆成不同意圖？)
對 Heading 作用，不調整位置，只做 #(Heading symbol) 數量的變化
對 paragraph 作用？什麼情況會需要？位置往後還是往前拉？
ex: 
```markdown
# Heading1
paragraph a
paragraph b
## Heading2
paragraph c
paragraph d

```
我想升降 paragraph 的意圖會是什麼？
升則往前拉，降則往後拉？
ex： 
想升 paragraph d 則變成
```markdown
# Heading1
paragraph a
paragraph b
paragraph d
## Heading2
paragraph c

```

想降 paragraph a 則變成
```markdown
# Heading1
paragraph b
## Heading2
paragraph a
paragraph c
paragraph d

```


#### 合併 Block 區塊
##### paragraph 併入 paragraph
，可以往上合併，也可以往下合併，
在同一 Heading 下合併應該做 **刪除2個 paragraph中間的 break line**

可以跳段落合併嗎？
ex:
```markdown
## Heading A
paragraph a
paragraph b
## Heading B
paragraph c

```
照理來說 a, b 2 段可以合併，b, c 不能合併
但以 Mind Map View 來看其實很方便，有時候可能就只是不小心在 Heading B 下面寫了可以併入 paragraph b 的描述。

跳段落合併應做 **調動位置**
調動 paragraph c 的位置到 paragraph b 後面

##### paragraph 併入 Heading
ex:
```markdown
## Heading A
### Heading B
paragraph a
paragraph b
## Heading C
paragraph c

```
呈現方式會是

```text
## Heading A - ### Heading B   - paragraph a
                               - paragraph b
## Heading C - paragraph c
```

如果使用者選擇 paragraph c 想往上與 Heading B 區塊做合併

則應該做的動作是 **調動位置**
調動 paragraph c 的位置到 paragraph b 後面

##### Heading 併入 paragraph
將 paragraph 納入此 Heading 底下
要考慮在同一群組？在不同群組？

### Drag And Drop
使用者拖拉可以改動 Heading 的層級

ex:
```text
               |- paragraph a
# Heading1_a - |- paragraph b

# Heading1_b

# Heading1_c
```

要如何拖動 # Heading1_c 才能讓他在 b 的下面 （變為 ## Heading2_c），而不是變成在 Heading1_a 下面?

如果拖拉跟 paragraph b （下方）重疊則調動位置在 paragraph_b 後方，
如果沒有跟 paragraph b 重疊則調動位置在 Heading1_b 後方

「預覽文字」
拖曳時直接顯示：
Move after paragraph b, become H2
或
Move after Heading1_b, keep H1

### 視圖切換
#### Mind Map View -> Markdown View
提供以使用者在 Mind Map View 選擇的 Block 為準，當切回 Markdown View 的時候讓 seleted 落在原本的 Block 顯示的區段位置。
### 區塊合併
提供將 paragraph, list, blockquote, callout 合併功能，
或 Heading 跟第一段 paragraph 合併？
可以先不用做，再思考一下這樣做是否有意義？
**但如果要做，則 Block 裡面可能包含不止一種 Context，使用者拖拉 Block 處理可能也會變複雜。**
**或者用 thematicBreak 當作客製化的合併區塊需求？**
